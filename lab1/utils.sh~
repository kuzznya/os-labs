#!/bin/bash

error_exit() {
    local script_name="$1"
    local message="$2"
    local code="${3:-1}"

    [[ -n "$message" ]] && 1>&2 && echo "Error in script $script_name: $message" ||
	    1>&2 && echo "Error in script $script_name"

    exit "${code}"
}

