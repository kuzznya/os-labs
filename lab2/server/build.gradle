plugins {
    id 'java'
    id 'application'
}

group 'com.github.kuzznya'
version '0.0.1-SNAPSHOT'

mainClassName = 'os.server.Main'

dependencies {
    implementation project(':utils')
}

jar {
    manifest {
        attributes(
                'Main-Class': mainClassName
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
